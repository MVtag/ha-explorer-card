import type { ExplorerCardConfig, ExplorerPresence } from "../models/config";
import type { HomeAssistant } from "../types";

export type ExplorerSetupState = "ready" | "attention" | "optional";
export type ExplorerEditorSection = "basic"|"rooms"|"presences"|"appearance"|"room-tools"|"zones"|"openings"|"room-reactions"|"room-actions"|"routes"|"route-graph"|"diagnostics";
export interface ExplorerSetupItem{id:string;label:string;detail:string;state:ExplorerSetupState;target:ExplorerEditorSection;}
export interface ExplorerEntityIssue{entity:string;source:string;target:ExplorerEditorSection;unavailable:boolean;}
export interface ExplorerSetupSummary{items:ExplorerSetupItem[];entityIssues:ExplorerEntityIssue[];attentionCount:number;configuredFeatureCount:number;roomCount:number;presenceCount:number;zoneCount:number;reactionCount:number;actionCount:number;routeCount:number;nodeCount:number;}
interface EntityReference{entity:string;source:string;target:ExplorerEditorSection;}
function cleanEntity(value?:string):string|undefined{const entity=value?.trim();return entity||undefined;}
function presenceHasPositionSource(presence:ExplorerPresence):boolean{const binding=presence.entity_binding;return Boolean(cleanEntity(binding?.room_entity)||presence.room_id||(Number.isFinite(presence.x)&&Number.isFinite(presence.y)));}
function collectEntityReferences(config:ExplorerCardConfig):EntityReference[]{const refs:EntityReference[]=[];for(const p of config.presences??[]){const primary=cleanEntity(p.entity_binding?.entity),room=cleanEntity(p.entity_binding?.room_entity);if(primary)refs.push({entity:primary,source:p.name??p.id,target:"presences"});if(room)refs.push({entity:room,source:`${p.name??p.id} · rum-tracking`,target:"presences"});}for(const room of config.rooms??[]){for(const reaction of room.reactions??[]){const entity=cleanEntity(reaction.entity);if(entity)refs.push({entity,source:`${room.name??room.id} · ${reaction.kind}`,target:"room-reactions"});}for(const action of room.quick_actions??[]){const entity=cleanEntity(action.entity);if(entity)refs.push({entity,source:`${room.name??room.id} · ${action.name}`,target:"room-actions"});}}for(const zone of config.zones??[]){const entity=cleanEntity(zone.state_binding?.entity);if(entity)refs.push({entity,source:zone.name??zone.id,target:"zones"});}for(const opening of config.openings??[]){const entity=cleanEntity(opening.state_binding?.entity);if(entity)refs.push({entity,source:opening.name??opening.id,target:"openings"});}for(const node of config.route_nodes??[]){const entity=cleanEntity(node.state_binding?.entity);if(entity)refs.push({entity,source:node.name??node.id,target:"route-graph"});}for(const edge of config.route_graph_edges??[]){const entity=cleanEntity(edge.condition?.entity);if(entity)refs.push({entity,source:"Betinget route edge",target:"route-graph"});}return refs;}
export function analyzeExplorerSetup(config:ExplorerCardConfig,hass?:HomeAssistant):ExplorerSetupSummary{const rooms=config.rooms??[],presences=config.presences??[],zones=config.zones??[],openings=config.openings??[],routeNodes=config.route_nodes??[],routeEdges=config.route_graph_edges??[],manualRoutes=config.routes??[],reactions=rooms.flatMap(r=>r.reactions??[]),actions=rooms.flatMap(r=>r.quick_actions??[]),references=collectEntityReferences(config);const entityIssues:ExplorerEntityIssue[]=[];if(hass){for(const ref of references){const entity=hass.states[ref.entity];if(!entity){entityIssues.push({...ref,unavailable:false});continue;}if(entity.state==="unavailable"||entity.state==="unknown")entityIssues.push({...ref,unavailable:true});}}const missing=entityIssues.filter(i=>!i.unavailable),unready=presences.filter(p=>!presenceHasPositionSource(p)),invalidRooms=rooms.filter(r=>r.points.length<3),floorplan=(config.image??config.background??"").trim();const items:ExplorerSetupItem[]=[{id:"floorplan",label:"Plantegning",detail:floorplan?"Plantegning er valgt.":"Vælg en SVG-, PNG- eller JPG-plantegning.",state:floorplan?"ready":"attention",target:"basic"},{id:"rooms",label:"Rum",detail:rooms.length?(invalidRooms.length?`${rooms.length} rum · ${invalidRooms.length} mangler en gyldig polygon.`:`${rooms.length} rum klar.`):"Tegn mindst ét rum for room-aware tracking og Living Rooms.",state:rooms.length&&!invalidRooms.length?"ready":"attention",target:rooms.length?"rooms":"room-tools"},{id:"presences",label:"Personer & objekter",detail:presences.length?(unready.length?`${presences.length} tilføjet · ${unready.length} mangler rum/position.`:`${presences.length} tracking-profil${presences.length===1?"":"er"} klar.`):"Valgfrit · tilføj personer, kæledyr, robotter eller objekter.",state:presences.length?(unready.length?"attention":"ready"):"optional",target:"presences"},{id:"entities",label:"Home Assistant-entities",detail:!references.length?"Ingen live entity-bindings endnu.":!hass?`${references.length} binding${references.length===1?"":"er"} · afventer Home Assistant.`:missing.length?`${missing.length} binding${missing.length===1?"":"er"} findes ikke i Home Assistant.`:entityIssues.length?`${references.length} bindings fundet · ${entityIssues.length} er midlertidigt unavailable/unknown.`:`${references.length} live binding${references.length===1?"":"er"} fundet.`,state:missing.length?"attention":references.length?"ready":"optional",target:missing[0]?.target??entityIssues[0]?.target??"diagnostics"},{id:"openings",label:"Døre & vinduer",detail:openings.length?`${openings.length} dynamisk${openings.length===1?" åbning":"e åbninger"} konfigureret.`:"Valgfrit · placér døre og vinduer og bind dem til kontaktsensorer.",state:openings.length?"ready":"optional",target:"openings"},{id:"routing",label:"Routing",detail:routeEdges.length||manualRoutes.length?`${routeEdges.length} graph edges · ${manualRoutes.length} manuelle routes · ${routeNodes.length} nodes.`:"Valgfrit · kortet kan bruges uden route graph.",state:routeEdges.length||manualRoutes.length?"ready":"optional",target:routeEdges.length?"route-graph":"routes"},{id:"living",label:"Living Rooms",detail:reactions.length?`${reactions.length} rumreaktion${reactions.length===1?"":"er"} konfigureret.`:"Valgfrit · lys, motion, media og åbninger kan gøre rummene levende.",state:reactions.length?"ready":"optional",target:"room-reactions"},{id:"quick-actions",label:"Rumhandlinger",detail:actions.length?`${actions.length} scene- eller scripthandling${actions.length===1?"":"er"} konfigureret.`:"Valgfrit · tilføj scenes og scripts direkte til rummets panel.",state:actions.length?"ready":"optional",target:"room-actions"},{id:"zones",label:"Dynamic Areas",detail:zones.length?`${zones.length} zone${zones.length===1?"":"r"} konfigureret.`:"Valgfrit · tilføj alarm-, rengørings- eller informationszoner.",state:zones.length?"ready":"optional",target:"zones"}];return {items,entityIssues,attentionCount:items.filter(i=>i.state==="attention").length,configuredFeatureCount:items.filter(i=>i.state==="ready").length,roomCount:rooms.length,presenceCount:presences.length,zoneCount:zones.length,reactionCount:reactions.length,actionCount:actions.length,routeCount:routeEdges.length+manualRoutes.length,nodeCount:routeNodes.length};}
