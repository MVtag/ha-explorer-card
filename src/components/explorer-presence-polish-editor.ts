import { LitElement, css, html, nothing } from "lit";
import { customElement, property } from "lit/decorators.js";
import type { ExplorerCardConfig, ExplorerPresence, PresenceObjectType } from "../models/config";

const PRESENCE_POLISH_LABELS: Record<PresenceObjectType, string> = { person:"Person · skoaftryk", pet:"Kæledyr · poteaftryk", robot:"Robot · hjulspor", vehicle:"Køretøj · dobbelte hjulspor", object:"Objekt · magisk spor" };

@customElement("ha-explorer-presence-polish-editor")
export class HaExplorerPresencePolishEditor extends LitElement {
 @property({attribute:false}) public config?:ExplorerCardConfig;
 private emitConfig(config:ExplorerCardConfig):void{this.dispatchEvent(new CustomEvent("config-changed",{detail:{config},bubbles:true,composed:true}));}
 private updatePresence(index:number,patch:Partial<ExplorerPresence>):void{if(!this.config)return;const presences=[...(this.config.presences??[])],current=presences[index];if(!current)return;presences[index]={...current,...patch};this.emitConfig({...this.config,presences});}
 private updateOptionalText(index:number,key:"color"|"icon"|"trail_color",rawValue:string):void{const value=rawValue.trim();this.updatePresence(index,{[key]:value||undefined});}
 private updateTrailDuration(index:number,rawValue:string):void{const value=Number(rawValue);this.updatePresence(index,{trail_duration:Number.isFinite(value)?Math.min(60,Math.max(1,value)):undefined});}
 protected render(){const presences=this.config?.presences??[];return html`<section class="panel"><div class="heading"><div><span class="eyebrow">Multi-person & Object Polish · Trail Controls</span><h3>Visuel profil for personer & objekter</h3></div><span class="count">${presences.length}</span></div><p class="intro">Personer og objekter på samme position spredes automatisk omkring deres room-anchor. Markør og bevægelsesspor kan nu tilpasses uafhængigt af hinanden.</p>${presences.length?html`<div class="profiles">${presences.map((presence,index)=>{const type=presence.type??"person";return html`<article class="profile"><div class="profile-heading"><div><strong>${presence.name??presence.id}</strong><small>${presence.id}</small></div><span class="type-badge">${PRESENCE_POLISH_LABELS[type]}</span></div><div class="grid two"><label>Farve (valgfri)<input .value=${presence.color??""} placeholder="Automatisk stabil farve" @change=${(e:Event)=>this.updateOptionalText(index,"color",(e.target as HTMLInputElement).value)}/><small>Farven på selve markøren. Tom = automatisk.</small></label><label>Ikon (valgfri)<input .value=${presence.icon??""} placeholder="Automatisk type-ikon" maxlength="8" @change=${(e:Event)=>this.updateOptionalText(index,"icon",(e.target as HTMLInputElement).value)}/><small>Bruges i den store markør; type-badget vises stadig.</small></label></div><label class="toggle"><input type="checkbox" .checked=${presence.visible!==false} @change=${(e:Event)=>this.updatePresence(index,{visible:(e.target as HTMLInputElement).checked})}/><span><strong>Vis på kortet</strong><small>Skjuler markøren manuelt; tracking-konfigurationen bevares.</small></span></label><div class="trail-box"><div class="trail-heading"><strong>👣 Bevægelsesspor</strong><small>${PRESENCE_POLISH_LABELS[type]}</small></div><label class="toggle"><input type="checkbox" .checked=${presence.trail_visible!==false} @change=${(e:Event)=>this.updatePresence(index,{trail_visible:(e.target as HTMLInputElement).checked})}/><span><strong>Vis spor</strong><small>Kan slås fra uden at skjule personen eller objektet.</small></span></label><div class="grid two"><label>Sporfarve (valgfri)<input .value=${presence.trail_color??""} placeholder="Samme som markør" @change=${(e:Event)=>this.updateOptionalText(index,"trail_color",(e.target as HTMLInputElement).value)}/><small>Fx #4b301d. Tom = markørens farve.</small></label><label>Varighed (sekunder)<input type="number" min="1" max="60" step="1" .value=${String(presence.trail_duration??4.2)} @change=${(e:Event)=>this.updateTrailDuration(index,(e.target as HTMLInputElement).value)}/><small>Hvor længe sporene falmer på kortet. 1–60 sek.</small></label></div></div></article>`;})}</div>`:html`<div class="empty">Tilføj først en person eller et objekt i sektionen ovenfor.</div>`}${presences.length?html`<div class="note">Sportypen vælges automatisk efter type. Reduced Motion deaktiverer bevægelsesspor, men markørerne forbliver synlige.</div>`:nothing}</section>`;}
 static styles=css`:host{display:block}.panel{display:grid;gap:14px;margin-top:12px;padding:16px;border:1px solid var(--divider-color);border-radius:12px;background:var(--card-background-color)}.heading,.profile-heading,.trail-heading{display:flex;align-items:flex-start;justify-content:space-between;gap:12px}.heading>div,.profile-heading>div{display:grid;gap:3px}.eyebrow{color:var(--secondary-text-color);font-size:.72rem;letter-spacing:.08em;text-transform:uppercase}h3{margin:0;font-size:1rem}.count,.type-badge{border-radius:999px;background:var(--secondary-background-color);color:var(--secondary-text-color);white-space:nowrap}.count{padding:5px 9px;font-size:.78rem}.type-badge{padding:4px 8px;font-size:.72rem}.intro,.note{margin:0;color:var(--secondary-text-color);font-size:.86rem;line-height:1.45}.profiles{display:grid;gap:10px}.profile{display:grid;gap:12px;padding:13px;border:1px solid var(--divider-color);border-radius:10px;background:var(--secondary-background-color)}.profile-heading small,label small,.toggle small,.trail-heading small{color:var(--secondary-text-color);font-size:.76rem;font-weight:400;line-height:1.35}.grid{display:grid;gap:10px}.grid.two{grid-template-columns:repeat(2,minmax(0,1fr))}label{display:grid;gap:6px;font-weight:600}input[type="text"],input:not([type]),input[type="number"]{box-sizing:border-box;width:100%;min-width:0;padding:9px 11px;border:1px solid var(--divider-color);border-radius:8px;color:var(--primary-text-color);background:var(--card-background-color);font:inherit}.toggle{grid-template-columns:auto 1fr;align-items:start;gap:9px;padding-top:2px}.toggle input{margin-top:3px}.toggle span{display:grid;gap:2px}.trail-box{display:grid;gap:11px;padding:12px;border:1px dashed var(--divider-color);border-radius:9px;background:var(--card-background-color)}.trail-heading{align-items:center}.empty,.note{padding:10px 12px;border-radius:9px;background:var(--secondary-background-color)}.empty{color:var(--secondary-text-color);text-align:center;font-size:.84rem}@media(max-width:600px){.grid.two{grid-template-columns:1fr}.heading,.profile-heading{align-items:flex-start}.type-badge{white-space:normal;text-align:right}}`;
}
